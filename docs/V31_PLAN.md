# V3.1 作战表与时间线(living doc;08-14 上午版)

判据权威 = `V31_PREREG.md`(已锁);证据权威 = `V3_FORECAST_DIAGNOSIS.md`;
本文只管**谁在什么时候干什么、每个门通向哪**。过期即改,改动留痕。

## 已完成(可追溯)

| 日期 | 事项 |
|---|---|
| 08-12/13 | v3 判决 + 五轮互审诊断:五病灶(两把尺子/时间反号/路由补贴+重复计费/状态混叠/local 混杂) |
| 08-13 | V3.1 四工单全部实现合入 main(奖励手术+观测+drain+门/标定/预注册);Java 63/0/0,Py 21/21 |
| 08-13 夜 | 标定件 v2(全 DC,μ=3.524/σ=2.512);真值表用真实 μ/σ 复跑通过 |
| 08-14 晨 | 四份判决落地:**drainfix 反转旧判决**(§2c);sp 种子分裂;track0 仪器证伪;PREREG §6 开关锁定 |

## 今天(08-14)

| 时段 | 事项 | 门/产出 |
|---|---|---|
| ~09:40 | track0b 收尾(历史使命已完成:证明 DC 级仪器二元) | 无判决力,归档 |
| ~10:00 | 翻模板开关 → 生成认证配置 → `preflight --v31-cert` | **过门才许开训** |
| ~10:30 | 武装 `run_v31_smoke.sh`(内置:机器空→cert门→installDist→真值表→100k s1→符号正自动接 s2→探针) | **P1 判决 ~14:00** |
| 并行 | 写 **slack-aware 逐任务 oracle**(§7.4 规格)≈2-3h 代码;冒烟结束后上机 ~40min | **考场上限判决 ~傍晚** |

**今晚的两门决定一切:**

```
P1(机制:配方能学会等吗)      × 上限(考场:等待值多少)
────────────────────────────────────────────────────
P1✓ 且 上限≥15%  → 今晚开 600k 全量(2臂×2种子)
P1✓ 但 上限<10%  → 配方有效、考场太浅 → 换场景参数(V3.1 代码全部照用)
P1✗(任一种子)   → 不调权重,明天起 temporal gate(诊断 §6d 步10)
上限 10-15%      → 灰区:600k 照开,但预期管理(P3 的 13% 门可能物理上够不着)
```

## 明后天

| 日期 | 事项 |
|---|---|
| 08-14 夜 ~ 08-15 夜 | 600k 全量:4 训练 ≈21h 串行(本地);GPU 机主表 s5/s6 约 08-15 收尾,之后可分流(**配对必须同机**) |
| 08-16 | 全量评测(`--local drain` 强制)→ **P2/P3 判决**;P3 过 → P4 扰动评测(+3-5h)= 终极验收"扰动会痛" |
| 08-17+ | 成功 → 写进论文 premise;P1 过 P3 败 → 调场景参数二轮;temporal gate 若启动 → 同样 100k 门制,不无限迭代 |
| 8 月底 | 蒸馏保底线(G2 Tier-3):无论前面成败,保证投稿前有一个"预报被用起来"的正面展示 |

## 悬置/背景项(不阻塞主线)

- **温度 gate 设计草案**:P1 失败才启动,规格已在诊断 §6c/§7.5(hold/balanced/release × spatial router)
- **方差缩减备胎**:组合里没有真降方差器(§6b);P1 败于噪声 → 逐决策候选均值中心化
- **论文线(Thread A)**:MDP 小节 6 处 find/replace(计划文件 snug-inventing-shore,**交付状态待确认**);CCA-PG 回补;导师 P2 腐蚀基线——GPU 机 08-15 空出后排
- 化石测试 test_observation_trimming(读不存在的 experiment_multi_dc_5)——投稿后清理

## 硬日期

| | |
|---|---|
| **09-18** | ICLR 摘要截止。V3.1/gate/蒸馏无论哪条线,此前必须有可写的结论(正或负+诚实局限) |
| **09-25** | 全文截止 |
| 缓冲核算 | 今天到摘要 35 天;每轮(改→冒烟→全量→判)≈3 天 → **最多还容 ~8 轮完整迭代,实际计划 ≤3 轮**(V3.1 → gate → 蒸馏) |

## 纪律回执(每轮必查)

预注册判据不挪门柱;两臂只差 forecast_mode(preflight 机械把门);评测一律 drain local;
跨臂只比物理量;完成率永不做筛选;checkpoint 全保留;watcher 重启必数实例数(坑⑪)。
