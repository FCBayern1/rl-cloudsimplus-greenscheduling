# 论文数字清单 —— 战役后照单替换

2026-08-23,5080。每一处结果数字的来源、所属构建、以及 G1–G3 战役之后的处置。

**为什么需要这份东西**:8-22 换审计表时,正文里 `0.182 / 0.184` 两个数被漏下,
和新附录对不上,是事后扫出来的。现在**整篇论文的数字要全换**,没有清单必漏。

## 污染源(两条,互相独立)

| 编号 | 污染 | 影响范围 |
|---|---|---|
| **C1** | **v4 构建**:`R_forecast` 因 BUG 3 是有偏常数、quarantine 因 BUG 1 基本惰性、Δr 因 BUG 2 恒零 | 仅 **EU-CRD 臂**的数字 |
| **C2** | **旧派发器**(`61043cf` 之前):`vm.getFreePesNumber()` 陈旧,每步派发起点错误 | **所有臂**的数字 |

C2 的实测幅度(A/B 探针,固定旧策略):碳 −0.69%、能耗 −9.89%、episode 长度 −14.65%。
C1 无法量化,因为命名通道在 v4 里根本不是它声称的量。

## 逐项清单

| # | 位置 | 内容 | 污染 | 战役后处置 |
|---|---|---|---|---|
| 1 | `tab:main` | 主表全部单元格(7 行 × 5 列) | C1+C2 | **全部重生成**。EU-CRD→knSV3b,Vanilla→matchedvan,12 种子,3 窗口 |
| 2 | `tab:main` caption | "medians over iso-eligible seeds"、ckpt 规则 | — | 改为 G1–G3 的规则(600k 最终 ck、seed 级配对 log-ratio) |
| 3 | §4 主段 | `0.269 / 0.196 / 0.255`、"5%"、`0.184 vs 0.185` | C1+C2 | 全换 |
| 4 | §4 DiD 句 | `+0.085 / +0.063 / −0.022` | C1+C2 | 全换 |
| 5 | §4 消融段 | `0.184 / 0.189 / 0.279` | **C2 only**(消融家族已是修复构建) | 重生成(仅因 C2) |
| 6 | §4 C_min 段 | `95.6 / 100` | C1+C2 | 全换;并按 SLA 门的新口径重述 |
| 7 | §4 审计句 | `0.178 / 0.185`、"twenty points" | C2(Vanilla 臂) | 重生成 |
| 8 | §4 开销句 | `2.1 ms` | C2(极小) | 可保留,建议重测 |
| 9 | `tab:perseed` (App D) | 4 种子 × 3 条件全表 + Δ | C1+C2 | **全部重生成**,种子改为 101–112 |
| 10 | App D 正文 | `+18.8 / −5.6 / +3.3 / +0.0`、`+1.7%`、`22.9 / 17.8%` | C1+C2 | 全换 |
| 11 | `tab:stoch-clean` (App E) | `0.379 / 0.397 / 0.443 / 0.451 / 0.464` | C1+C2 | **重生成**(见下「拍板记录 D2」)。加入 G1 预注册,身份为 secondary/descriptive,不做显著性检验 |
| 12 | App E 正文 | `14%`、`5%`、`0.192 vs 0.187`、`0.480 / 0.483 / 0.50–0.51` | C1+C2 | 同上 |
| 12b | §4「Two boundaries」段 + §4.1 setup「14% under sampling」 | 依赖 App E | C1+C2 | 随 App E 一起换;这两处是 App E 的正文承重点,漏换即自相矛盾 |
| 13 | App F | 失效种子 `91–96% / 98%`、`σ² 7.2 vs 3.2`、`max w≈1.15`、`σ(w)≈0.33`、`0.20–1.00` | C1+C2 | **全部重生成**。σ² 与 ρ 的诊断在 v4 上本就测的是坏机制 |
| 14 | App G + `fig:mechanism` | 训练诊断图与 `0.87 / 0.2–1.0 / σ(w)≈0.33` | C1+C2 | **图需重画**。0.87 正是 BUG 1 的病征 |
| 15 | `tab:ablation` (App H) | base/gate-open/no-reweight × 3 条件 | **C2 only** | 重生成(仅因 C2) |
| 16 | App H 正文 | `23%`、`0.283 / 0.275 / 0.205 / 0.248`、`99.46 / 99.42`、`0.227→0.213`、`10–13%` | C2 | 全换 |
| 17 | `tab:auditor` (App I) | 10 格网格 + χ + 开火率 | C2(Vanilla 臂) | 重生成;并按 W4 换成健康 EU-CRD ckpt |
| 18 | App I 正文 | `+0.71 / −0.71 / 0.00 / +0.23`、`14.9% / 99.4% / 0.5%`、`2.3% / 0.9pp` | C2 | 全换 |
| 19 | `tab:overhead` (App J) | 延迟/RSS/wall/70× | C2(极小) | 建议重测,不重测亦可 |
| 20 | `tab:testbed` (App A) | DC 配置、碳强度、涡轮编号 | — | **不变**(配置,非结果) |
| 21 | App A 工作负载段 | 8000 作业、MI 分位、deadline 中位 | — | **不变** |
| 22 | App B | 超参、`600k`、`5–6 h` | — | 更新种子集合、episode 口径、新 jar/commit |
| 23 | 摘要 | "5% lower carbon" | C1+C2 | 换;并按抗波动框架重写(任务 2) |
| 24 | 贡献点 3 | "5% lower carbon"、"only compared method" | C1+C2 | 同上 |
| 25 | 结论 | "degrades 5% less"、"one seed in four" | C1+C2 | 同上 |

## 不受污染、可直接留用

- `tab:testbed`、工作负载统计、超参数(#20–22):**配置而非结果**
- 引用、相关工作、§3 算法描述:8-23 已按真实实现重写
- 页数合规(9 页)、引用完整性(44 个)

## 新增、战役后才有的

| 项 | 来源 |
|---|---|
| A/B 派发器探针注记 | `reports/AB_DISPATCH_PROBE_2026-08-23.md`(碳 −0.69%,能耗 −9.89%) |
| 三个分层风电窗口 | `drl-manager/calib/p0c_green_windows.json` |
| 训练窗口的绿电稀缺度 | k=0 平均风功率 1419 kW = 全年 **p10** |
| SLA 门 / McNemar | Codex P0 判据 |
| forecast-dropout 基线 | 重跑,3 种子,排在战役最末(见「拍板记录 D3」) |

## 替换纪律

1. 换完后跑一遍 `\ref`/环境/引用校验(脚本见本仓 git log 中的检查片段)。
2. **正文数字必须回指表格**:每换一个表,grep 正文里该表的所有数字。
3. 页数会变,9 页限制要重查。
4. 任何"未重生成即保留"的数字,必须在 caption 或正文注明其来源构建。


## 拍板记录(2026-08-23)

三件在数据回来之前就能定的事,已定。

**D1 主图重画。** 原 drawio 全图不只是密,它的 ② 号框仍画着 `R_forecast` 的未归一形式,
和改过的 Eq.~2 直接矛盾,另有一个空的 "Key note" 框。没有 `.drawio` 源文件可改。
处置:正文换成新画的简版(`paper_latest/figs/make_fig_framework.py` → `fig_framework_main.pdf`,
六个块、两种配色、训练/部署两条泳道),旧 PNG 退役不进附录 —— 附录「全版」的角色由
Algorithm~\ref{alg:eucrd} 承担,它比那张图更精确。绘图脚本进仓,以后跟着正文改。

**D2 采样解码附录保留并重生成。** 它在正文承重四处:§4.1 的 "14% under sampling"、
§4 的解码边界段、以及把过度延迟失败定位到 mode selection 的两处(App F、App G)。
删掉等于同时丢掉一个诚实边界和一条诊断。而重跑它**不需要重训**:同一批 checkpoint
加 `--stochastic` 重评即可,按 P0-C 实测 ~610 s/cell,12 种子 × 2 臂 × 3 条件 ≈ 12 h 无人值守评测,
可挂 3060。预注册身份写死为 **secondary, descriptive**,不做显著性检验,这样加进去不动统计设计。

**D3 forecast-dropout 基线重跑,3 种子,排最末。** 审稿人点名的对照,在 limitation 里认掉
等于把同一条意见留到下一轮。3 种子 ≈ 21 h,是 168 h 主链的 12%。它只是 §4.1 的动机数字
(「forecast 值 5%」),不是头条,所以正文按区间而非点值写。排在主臂之后,一旦时间不够,
它是第一个被砍的,而不是头条。
