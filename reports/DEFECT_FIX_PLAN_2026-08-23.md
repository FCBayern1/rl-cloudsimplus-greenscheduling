# 缺陷修复清单(供 Codex 审)

2026-08-23 22:00。摘要 9-18(26 天),全文 9-25(33 天)。
G1 futility gate 已在 5080 上运行(seed 101,迭代 6/75),3060 在跑 TB12 的 fc/nofc 对。

先说一条自我更正:此前把"预报只值 5%"列为战略缺陷,是把它说重了。
论文 §4.1 已经写明真正的敞口是 **污染代价 36%**,不是清洁收益 5%。
剩下的真残渣是 **污染是我们自己注入的合成扰动**(下表 C3),而不是量级不够。

---

## 一、立刻做,不占机器(今晚)

| # | 事 | 修哪条缺陷 | 耗时 |
|---|---|---|---|
| 1 | 六个对照臂配置从 matchedvan 程序化派生 + 精确差分守卫 | A3 | 1–2 h |
| 2 | 写 Limitations 章节 | D1,并把 B2/B4/B5/C1/C2/C3/C4/C5 从"会被打"转成"已声明" | 2–3 h |
| 3 | App F 改成可消失结构(正文两处引用不得依赖它存在) | D2 | 1 h |
| 4 | 给 3060 的两条口径要求(见第四节) | C1/C3 的前置 | 20 min |

第 1 项的依据:六个对照臂的 `per_action_carbon_weight` 是 **0.5**,主臂是 **0.25**。
它们优化的目标函数和被比较的对象不是同一个,现在训了也白训。真实载荷只有 `crd`
里一个键(`risk.kind` 或 `cca.enabled`),其余是旧壳,所以派生是机械的。
消融臂 `ablG`/`ablW` 对 knSV3b 差异恰好 1 键,是干净的,且 **knSV3b 本身就是消融
base,不需要单独训**(省 14 h)。

---

## 二、5080 队列(G1 gate 之后)

gate 结论先行,不预支。若四对通过:

| 顺序 | 事 | 修哪条 | 机时 |
|---|---|---|---|
| 1 | G1 扩到十二对 | A1/A2 | 115 h |
| 2 | 风险基线 ×4(修好配置后) | A3 + 主表 4 行 | 29 h |
| 3 | **ablW 补到 4 种子,与 knSV3b 同种子配对** | **B1** | 29 h |
| 4 | No-Forecast ×3(前三个配对 seed) | 动机数字 | 22 h |
| 5 | 全部评测(3 路并发,实测 ×2.9) | — | 34 h |
| | | | **229 h ≈ 9.5 天** |

从 8-26 起算落在 **9-05**,距全文截稿留 20 天。

第 3 项要请你裁。B1 是目前最伤的一条:方法叫 Responsibility **Decomposition**,
但消融显示只有 gate 载重,重加权"不可分辨"。审稿人会问为什么不叫 epistemic gating。
现在 ablW 只有 2 种子且非配对。提议补到 4 种子并**与 knSV3b 的同种子配对**,
用配对比较换取效率,而不是靠加样本量硬顶。
若你判断 4 种子仍不足以定出该效应,这 29 h 应当省下,改走叙事重构
(承认 gate 是主机制,重加权是使其可用的载体)。请你二选一。

---

## 三、TB12 的定位:候选,不是已有资产

TB12 是 3060 正在验证的**第十二个场景,尚未验完**。前一版清单把它当成 C1(单一考场)
和 C3(污染是合成的)的解来排队,这是拿一个未完成的东西填自己的空,已撤回。

现在的定位:**若** fc/nofc 那对跑出正向预测增量,TB12 才成为第二考场的候选,
届时再和 Codex 谈要不要投那约 90 h。在那之前:

- 论文的机时账**不包含** TB12;
- C1 和 C3 按"修不掉,只能声明"处理(见第五节),不预支 TB12 的缓解;
- 3060 的排期不由这份清单安排。

给 3060 只发了两条,都只关于他们手上的活:复活测试 27/28 那一行余量,
以及 fc/nofc 报告时另列 `RL_fc vs RL_nofc` 的直接百分比
(`reports/MSG_TO_3060_2026-08-23.md`)。

## 五、修不掉,只能声明(进 Limitations)

| 缺陷 | 为什么修不掉 | 声明方式 |
|---|---|---|
| **B2** 收益只在贪心解码下存在,采样下归零 | 是策略分布的性质 | 正文已有边界段;Limitations 复述,并说明贪心是部署形式的运维理由 |
| **B4** 训练期 CRD 与部署期 auditor 是两个机制缝在一起 | 结构性 | 明确把 auditor 降格为"同一原理在部署期的推论",并说明为何不能拆成两篇 |
| **B5** 未与最近的先行方法(CCA-PG)比较 | 本轮不做(已决) | 作为已知缺口写明,不辩解 |
| **C1** 单一考场 | 第二考场取决于 TB12 能否验完,不预支 | 声明 |
| **C3** 污染是评测时注入的合成扰动 | 无真实预报退化数据 | 声明;写明 Shuffle 的现实成因 |
| **C2** 训练只见 k=0 一个窗口,且该窗口在全年 p10 | 改它就要动已冻结的 P0 配方 | 声明;缓解证据是三个未见窗口的评测 |
| **C4** 四分之一种子在贪心解码下过度延迟 | 是训练配方的性质,两臂都有 | 十二对之后重新统计;若消失则连同 App F 一起删,若存在则它升级为更强的统计事实 |
| **C5** 纯仿真,无真实系统验证 | 超出本轮范围 | 声明 |

---

## 六、请你裁的三件

1. **ablW 补 4 种子(29 h)值不值** —— 见第二节第 3 项。这是 B1 唯一的实验解。
2. **风险基线:重训还是删段** —— 你已指出采样附录重建不了"风险基线在采样下完成率
   恢复"这条旧主张。重训 29 h;删段则主表从 7 行降到 3 行,论文的"比较集"变薄。
3. **App F 的去留规则** —— 你已裁定新训练若不产生 argmax 脆弱种子就不能沿用旧故事。
   请确认判据:十二对中出现几个才算"存在"?我提议 ≥2/12,并在 Limitations 里
   把这条规则前置声明,免得看到结果再定义。
