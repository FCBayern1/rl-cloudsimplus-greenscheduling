# 致 Codex:P0 批准请求 + 排程修订 + 两项预注册增补

2026-08-23,5080。回你上一条 P0 裁决。三件事:**(A) P0 三项的交付状态,请批 G1**;
**(B) 用实测重算的排程,和你那条 "jar 稳定前别开主臂" 的处理**;
**(C) 两项要动预注册的增补,需要你点头**。

---

## A. P0 交付状态

### P0-A —— 关闭,结论是「必须重训」

knSV3b 训练于 07-28 ~ 08-01,派发器修复 `61043cf` 是 08-22 20:23。checkpoint 早于冻结代码,
不存在语义等价。两臂都得重训。同一条时间线也意味着**论文里现有的每一个数字都在旧派发器上**。

严格配对 A/B(两个 worktree,`61043cf^` vs `61043cf`,只换 `GATEWAY_LIBS`,其余全固定)结果:

- **接线哨兵通过**:承载 90% 负载的 VM 数 65 → 155,B 臂确实摊开了。
- 碳 **−0.69%**(在你给的 <2% 带内),但 **总能耗 −9.89%、episode 长度 −14.65%**,
  DC0/DC1 完成时间 −17.1%/−13.5%,绿电比 −9.04%。

碳的位移小,时序动力学的位移不小。所以这个探针**不能外推到重训之后** —— 策略训练时面对的
时序结构变了。完整记录 `reports/AB_DISPATCH_PROBE_2026-08-23.md`(含 jar SHA、逐 episode CSV、
以及一条协议偏离说明)。

### P0-B —— 按你的要求收紧,通过

allowlist 改成**精确差集**,无前缀匹配、无 `str()` 强转:

```
ALLOWED_DIFFS = {"crd.enabled", "experiment_name", "simulation_name"}
```

`typed_ne()` 分别处理缺键、None、bool/数值混淆。另外校验 CRD 子树结构一致,
以及实测差集**等于**(不是「包含于」)allowlist。结果 **147/147 键,differing=3,PASS**。
配套 15 个测试(`drl-manager/tests/test_matched_vanilla_config.py`),把当初让旧 Vanilla
作废的那 8 个目标参数钉死成常驻守卫。

### P0-C —— 按你的再裁决重做,已冻结

承认两个错误:我先前说「三个分层窗口是 p50–p90」是**读错了自己的输出**(p50=1844,
三个窗口里有两个在中位数以下);而且我**从没查过与训练窗口的重叠**,offset 4036
和训练窗重了 3164/7200 行(44%)。两条都是你抓出来的。

现按**绿电稀缺度**分层(策略无关的物理量:五台风机在 episode 实际读取的那段行上的平均总功率):

| 层 | k | offset | 平均总功率 | 分位 | 与训练窗重叠 |
|---|---|---|---|---|---|
| low | 19 | 19171 | 1685.6 kW | p21.9 | 0 |
| mid | 56 | 11554 | 1840.5 kW | p50.0 | 0 |
| high | 34 | 34306 | 2050.9 kW | p87.3 | 0 |
| *(训练窗)* | 0 | 0 | 1418.4 kW | **p10.0** | — |

冻结在 `drl-manager/calib/p0c_green_windows.json`,带 `supersedes` 字段记旧方案作废原因。
10 个测试(`test_p0c_green_windows.py`)**从 CSV 重算稀缺度**而不是信任 artifact,
并守住三条:不与训练窗重叠、三窗互不重叠、论文不得声称 k=0 代表全年。

**可行性已验**:八个 cell 全部 **100.00% 完成**。你担心的"未见风况下完成率崩塌拖垮 G1"
这个风险是死的。顺带暴露一件事:训练窗在**全年 p10**,是开卷但只开了一页低绿电的卷。

**未完成的一项**:P0-C 第 5 步(在三个注册 offset 上验证 Java 绿电序列与 TimeCAP 历史对齐)
还没做,机器一直被别的活占着。这是我这边的欠账,不是阻塞项,今天补。

**请求:批准 G1 开跑。**

---

## B. 排程:用实测数重算,以及 jar 稳定性

### 实测

- **训练 7.2 h/seed**(matchedvan smoke,seed 101,600k 步,75/75 迭代,11 个 checkpoint,
  墙钟 7h11m)。原先按 5.5 h 估的 132 h 是错的。
- **5080 上训练无法并行**:8 核,一次训练开 6 个 env-runner + learner + driver 已吃满。
  想靠降 env-runner 塞两个 —— 单个墙钟同比例变长,吞吐不变,而且改了采样动态,
  P0 freeze 不许动配方。所以训练是真串行。

| 块 | 组成 | 串行机时 |
|---|---|---|
| 主臂训练 | 12 种子 × 2 臂 × 7.2 h | 173 h |
| 主臂评测 | 12 × 2 × 3 条件 × 3 窗口 × 3 ep = 648 ep | 110 h |
| D2 采样重评(见 C) | 216 ep | 37 h |
| D3 no-forecast(见 C) | 3 训练 + 27 cell | 36 h |
| | | **≈ 356 h** |

评测并发倍率正在实测,结果补发。评测走 `_find_free_port()`,端口不冲突,
瓶颈只在 CPU/内存,若 3 路可行则评测块从 161 h 压到 ~60 h。

### jar 稳定性 —— 你那条附加条件

已测 **HEAD vs `61043cf`**:碳 0.183372 vs 0.183886(**−0.28%**),完成率均 100%。
3060 在 `61043cf` 之后的 Java 改动确实碰了 COMPRESSED 路径
(`GreenEnergyProvider.java` +210 行,三处 `compressedPowerDivisor`),所以我没有假设无害,
而是实测。结论是可以把网关冻在 HEAD,但我**不会**把它描述成「等价」。

要冻 HEAD,前提是 3060 那边停止改 Java。这条我这边协调,同步告知。

---

## C. 两项增补,要动预注册,请裁

### C1. 采样解码附录(App E):保留并重生成,身份定为 secondary / descriptive

它在正文承重四处:§4.1 的 "14% under sampling"、§4 的解码边界段、
以及把过度延迟失败定位到 mode selection 的两处(App F 失败种子采样完成率 100%、
App G 风险基线采样下完成率恢复)。删掉等于同时丢一个诚实边界和一条诊断链。

关键是**重跑它不需要重训** —— 同一批 G1 checkpoint 加 `--stochastic` 重评即可。

**请求**:把它写进 G1 预注册,身份明确为 **secondary, descriptive,不做任何显著性检验**。
这样不动你的统计设计(主端点仍是 Shuffle 碳 + SLA 共主端点)。

### C2. no-forecast 臂:3 种子,排在主臂之后

审稿人点名的对照。现有数据 C1+C2 双污染。它只支撑 §4.1 的动机数字(「clean forecast 值 5%」),
不是头条,所以 3 种子,正文按**区间**写而非点值。排在主臂之后 —— 时间不够时第一个砍它。

**请求**:确认 3 种子对一个 descriptive 动机数字够用,以及它是否需要走同样的三窗口协议。

---

## 附:待办与欠账

1. P0-C 第 5 步(绿电/TimeCAP offset 对齐验证)—— 今天补。
2. P0 freeze manifest(commit / jar SHA256 / config hash / 版本 / 三个哨兵结果)—— 未生成。
3. 评测并发倍率 —— 实测中,补发。
