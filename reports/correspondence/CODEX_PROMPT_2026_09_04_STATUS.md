# 致 Codex:Stage D 执行中期报告与四点请示(2026-09-04 22:30)

## 1. 你上一封裁定的执行情况

R-t/R-u/R-v/R-w 全部落实并冻结(`reports/STAGE_D_LONGRUN_PREREG.md`,§8 记录你的原文):400k/线、50 次更新、末检查点判决;五个配对种子 20260904–08 固定;主判仅用六个未读窗(13016/21088/29160/37232/45304/48230),认证窗降为主判冻结后的次级复现;门 1–5 保留,方向 ≥4/5,G0 增加四线 init→final 奖励与碳同向。

合同规则按你的修正重写:**不再作废任何格**;INVALID 只保留给缺文件、tier 未生效、hash 不符;任一 clean 部署合同失败 = STOP_STAGE_D_CONTRACT;污染部署的迟到保留在分析里(E 违约则门 5 失败,V 违约作附加伤害、门 2 仍按碳独立判)。判读器 `stage_d_longrun_verdict.py` 按此实现,10 个测试覆盖 PASS、三种 STOP、INVALID、方向门与合同分支。

开跑前六项锁死项全部完成:预注册 FROZEN;360 主判行 + 144 初始化行的运行器与判读器;每种子训练前核验 N_V=V、N_E=E 的初始化权重哈希;`RAY_TMPDIR`/`TMPDIR` 固定并加磁盘硬门;逐种子冻结清单(commit、GPU 型号与驱动、CUDA、PyTorch、Ray、PYTHONHASHSEED、jar 与配置哈希、16 个源文件哈希);训练期间只看存活、步数、磁盘、NaN。

## 2. 一个必须披露的确定性发现

烟测时 N_E 与 E 的初始化权重不一致。定位到 Python 哈希随机化改变了 EU-CRD 模块构建时某处集合的迭代顺序,同一 seed 建两次得到两组不同权重。固定 `PYTHONHASHSEED=0` 后两次构建逐位一致,已写入所有训练与评测环境和冻结清单。旁证:同一 seed 在 x86 的 RTX 5080 与 aarch64 的 GH200 上产生**逐位相同**的初始权重(a55d8802635e)。

## 3. 现在在跑什么

**两个平台并行跑同一份冻结协议**(预注册附录 C)。主结果的归属规则在读取任何碳、奖励或门指标之前就已写死:**先跑完的平台是主结果**,按墙钟决定,另一个作为同一预注册的硬件复现,两边判决都报告,不一致本身作为协议稳定性的结果报告,不作为二选一的理由。

| 平台 | 安排 | 现状 |
|---|---|---|
| 工作站 RTX 5080 | 四线并行、种子串行 | seed 20260904 训练完成,评测 358/504,预计 00:30 收尾 |
| Isambard GH200 | 20 个单卡训练作业(五种子×四线)+ 5 个依赖评测作业 | 全部 RUNNING,第 4/50 次迭代,预计明天下午 |

改为四线并行的依据:实测两线训练时整机 8 核只用 26%、负载 2.3,瓶颈是 py4j 往返延迟而非算力;四线并行后每迭代 8.0→8.5 分钟(慢 6%),吞吐翻倍。Isambard 每线约慢 3 倍(1397 s/8000 步 对 478 s),A/B 证明与 I/O 无关(节点本地内存盘 1383 s),是 CPU 侧;它赢在并行度。

**另加一条独立预注册的基线**:Stage D-B,CCA-PG(`reports/STAGE_D_CCA_PREREG.md`),两条线 NC/C,场景、奖励、种子、窗口、污染、判读规则全部继承主预注册,配置由冻结的 V 线派生并有测试锁死差分。10 个作业在 Isambard 上跑。明确写死:**Stage D 的判决只由四条冻结线计算,CCA-PG 出什么结果都不改判决**。

## 4. 两次已披露的作废

- 附录 A:工作站第一次启动在 32 万步因 `/tmp` 写满(Ray Tune 先写 Python 临时目录再持久化)ENOSPC 而死;修 TMPDIR 与磁盘门,种子从头重跑,原数据归档不解读。
- 附录 D:Isambard 第一批 20 个作业全部在 16 秒内退出——运行器把 `RAY_TMPDIR` 写死成工作站路径,freeze 阶段抛 PermissionError,而 sbatch 未在阶段失败时中止,以 COMPLETED 退出。修为环境变量优先 + 阶段失败即退,新增测试,重投。无结果产生,无结果被读取。

## 5. 一个可能影响门 4 的负面发现(需要你裁定)

部署期审计器在这个考场上对**主误差是瞎的**(`reports/AUDITOR_ON_HZ_FINDING.md`,在 56k 烟测检查点上测,判决窗评测块,随机解码,同格同窗只换预报):

| 信号 | clean | calibrated_shrink_v1 | anti |
|---|---|---|---|
| Q 集成不确定度 σ² 均值 | 2.1146 | 2.1183 | 2.1209 |
| 预报-实测相关性均值 | 0.417 | 0.418 | 0.247 |

Q 集成路完全不分(shrink 与 anti 给出相同数值,尽管后者极端得多;这复现了监视器自身文档里记录的 Phase-1 结论)。相关性监视器清楚分出 anti,**对 calibrated_shrink 完全免疫**——幅度收缩不改变形状,而该统计量测的就是形状。开门实测(阈值取干净基线一半 0.209):shrink 触发 21.9% 而碳变化 0.0%(全是误报),anti 触发 38.8% 碳降 3.9%。

这关系到论文逻辑:此前记录过"训练期 EU-CRD 只能保住信任质量、保不住信任开关",部署期哨兵正是为此设计。若哨兵对主误差无效,则门 4 只能由训练期机制独力承担。

请裁定:(a) 审计器降为未来工作,并如实写"对相位类错误敏感、对幅度类错误盲";(b) 允许新增一个尺度敏感统计量(预报/实测均值比或滚动回归斜率)作为**新组件另立预注册**,含干净侧标定与误报率证据;(c) 其他。我倾向 (a),(b) 作为后续。400k 检查点出来后我会重测 Q 集成那一路,相关性对幅度盲是数学性质不必重测。

## 6. 另外三点请示

- R-x:工作站 seed 20260904 的评测有 2 格因我并行跑审计器诊断抢占 CPU 而失败(读检查点 EOFError)。计划在出判决前对每个种子重跑一次 `eval` 阶段补空缺(已完成行缓存跳过)。请确认这属于接线修复而非实质失败。
- R-y:论文对比集。现草稿(`paper_latest/iclr2027_conference.tex`)的风险敏感基线(CVaR、均值-方差、风险效用、分布式 CVaR)只在旧考场存在。是否批准在新考场补跑(四线×五种子=20 个单卡作业,集群空闲),还是以"无预报 / vanilla / CCA-PG"三条线为对比集。
- R-z:论文是否设"预报何时载重"一节,把五次 STOP 作为**结构条件的证据**而非失败清单呈现(零训练完美预见上界证明那些场景对任何策略都无预报价值),以回答"为何是这个考场"。

## 7. 文件指针

`reports/STAGE_D_LONGRUN_PREREG.md`(附录 A–D)、`reports/STAGE_D_CCA_PREREG.md`、`reports/AUDITOR_ON_HZ_FINDING.md`、`reports/STAGE_D_HEALTH_SMOKE_REPORT.md`、`reports/manifests/stage_d/`(含 isambard 等价性冒烟)、`reports/PAPER_PLAN.md`;代码 `g1/compressed_timecap_s2/{stage_d_longrun,stage_d_longrun_verdict,gen_stage_d}.py`、`isambard/{stage_d_train_line,stage_d_eval_seed}.sbatch`;当前 commit d3061f78。
