# 本机首对训练日志复核：seed 20260911

2026-09-10。只读分析已完成的本机 `matched_pair/V_s20260911` 和
`matched_pair/E_s20260911`。没有运行部署评测、读取其他种子的效果、修改配置、
加载检查点执行策略或改变任何判据。本文不是效果判决，也不决定主结果平台。

## 结论

预算完成，核心训练统计均有限，训练记录中的作业完成率接近/等于 1。
两条线的价值估计有改善、策略分布有所收紧；训练回报仅有有限改善证据，
不能据此判断 EU-CRD 比 Vanilla 更省碳。EU-CRD 在第 5–15 轮持续施加重加权，
后悔非零样本上的责任份额没有消失。实际作业分布也排除了本次训练只使用 DC0
这一特定退化假设。准时率、逐 ID 合同和确定性部署收益仍由正式评测负责。

## 数据范围与预算

| 项目 | V | E |
|---|---:|---:|
| 完成迭代 | 15 | 15 |
| 环境步 | 120000 | 120000 |
| monitor 已完成回合 | 184 | 185 |
| 迭代用时合计 | 12285.45 s（3.41 h） | 23822.04 s（6.62 h） |
| 平均每轮 | 819.03 s | 1588.14 s |
| 最小工作负载作业完成率 | 1.0 | 1.0 |
| 最小 MI 完成率 | 0.9999999999999998 | 0.9999999999999998 |

实测时间比 E/V ≈ 1.939。逐条检查了 `policy_loss`、`vf_loss`、
`vf_explained_var`、`entropy`、全局梯度范数，15 轮均无非有限值。
CSV 中旧 DEFER 专用诊断列仍有 `nan` 缺失值；不能把这些缺失值解释为训练发散，
也不应声称整个 CSV 没有任何 NaN。

## 回报：变化小，且必须区分统计口径

RLlib `env_runners.episode_return_mean` 是汇总回报，不是独立部署碳指标，
也不能与自定义 `global_agent_reward` 或某个全局代理回报字段混用。

| RLlib 回报读数 | V | E |
|---|---:|---:|
| 首轮 → 末轮 | -32.582 → -32.256 | -33.368 → -31.812 |
| 前三轮均值 → 后三轮均值 | -32.665 → -32.325 | -33.176 → -32.012 |
| 15 个迭代读数范围 | [-33.168, -32.066] | [-33.641, -31.812] |

另一种明确分段的读法：monitor 的前 36 个完整回合与后 36 个完整回合，
`episode_reward_after_lagrangian` 均值分别为：

- V：-33.108 → -32.255；
- E：-32.968 → -32.605。

两种摘要的变化量不同，不能从中挑选最有利的一种证明优劣。后者单回合的范围
约为 V [-110.14, 18.17]、E [-109.91, 22.50]，远宽于迭代汇总曲线。
这说明“汇总回报在窄带波动”不代表所有回合表现一致。不同训练窗口、随机动作、
汇总窗口和策略变化均可能影响这些读数；本次没有做因果分解。

## 价值估计与策略分布

| 首轮 → 末轮 | V | E |
|---|---:|---:|
| 全局熵 | 0.24849 → 0.18889 | 0.25824 → 0.18176 |
| 价值解释方差 | 0.32009 → 0.74287 | 0.29546 → 0.89398 |
| 归一化价值损失 | 0.84915 → 0.10075 | 0.82200 → 0.08639 |
| 原始价值 MSE | 2.43964 → 2.17421 | 2.26100 → 1.78584 |
| 目标方差 EMA | 2.81 → 21.58 | 2.75 → 20.67 |

价值解释方差的前三/后三轮均值：V 0.527 → 0.821，E 0.452 → 0.853。
这些读数支持价值估计有所改善，但任务分布并非固定验证集。归一化损失的大幅下降
伴随着分母增加约 7.5 倍，不能说原始价值误差下降了八九成。

熵下降说明记录的动作分布更集中，不能仅凭熵的绝对值判定无塌缩；
合法动作数、空槽与真实作业必须分开。这里另用完成作业的 DC 计数核验空间活性：

| 完成作业分布 | DC0 | DC1 | DC2 |
|---|---:|---:|---:|
| V：6440 个作业 | 37.81% | 43.21% | 18.98% |
| E：6475 个作业 | 37.33% | 44.42% | 18.25% |

两线实际均使用三个站点；DC3/DC4 为零。这不能证明路由最优，也不能替代
按有效槽位统计的开工偏移或策略分歧分析。

## EU-CRD 是否持续工作

`reweight_applied` 第 1–4 轮为 0，第 5–15 轮为 1，符合 450 次调用预热。
以下是第 5–15 轮日志快照的均值与范围，不是汇总全部训练样本后重新计算的统计量：

| 指标 | 11 轮均值 | 范围 |
|---|---:|---:|
| 有效转移中后悔非零比例 | 3.00% | 2.26%–3.53% |
| 全局预报责任份额 | 1.33% | 0.91%–1.75% |
| 后悔非零处的预报责任份额 | 44.38% | 36.53%–53.90% |
| 原始非零信号通过异常门的比例 | 46.42% | 37.88%–56.34% |
| 实际权重：正优势均值 | 1.00126 | 0.99929–1.00355 |
| 实际权重：负优势均值 | 0.99835 | 0.99605–1.00080 |

末轮后悔非零比例 3.125%，对应条件责任 40.74%，全局责任 1.273%。
条件责任已包含异常门清零的影响，不能再额外乘一次通过率。
记录到的 `reweight_w_std` 仍为根据责任份额重建的护栏前标准差；
不能把它当作直接记录的最终权重标准差。

负优势平均权重略低于 1，但平均值不足以排除少数关键纠错样本被显著压低。
本次支持通道持续工作，不支持它的效果有益或无害。

全局梯度范数的迭代记录范围：V 3.72–34.17，E 3.54–52.15。
配置含全局范数裁剪 20；不能用每轮记录的单个数推导所有优化调用的裁剪频率。
没有更细的裁剪记录就不报告实际裁剪比例。

## 检查点开销

只枚举文件，不加载权重执行推理。两个末目录均为 `checkpoint_000014`：

- V：27701887 bytes；全局 `module_state.pt` 3323680 bytes；
  learner `state.pkl` 10025381 bytes。
- E：511776084 bytes；全局 `module_state.pt` 244897860 bytes；
  learner `state.pkl` 251617606 bytes。

训练日志登记的全局模块参数：V 可训练 828650；E 可训练 31025130，
另有不可训练 30196480。完整检查点约大 18.5 倍，包含模块权重和学习器状态等。
不能把这整个体积直接称为部署演员必须承担的内存，也不能仅归因于 Q 头权重文件。

## 下一步

不据本机一个种子的训练趋势改任何参数、预算、检查点或平台归属。
按冻结规则完成三对及确定性部署评测。最终重点是配对绝对碳、干净性能与履约，
并保留 cover_argmax 参照。训练活性与价值估计改善不等于终端碳改善。

## 来源和校验和

V 运行目录：
`drl-manager/logs/matched_pair/V_s20260911/multidc_gtrxl_training/PPO_multidc_env_db752_00000_0_2026-09-09_13-06-15/`

E 运行目录：
`drl-manager/logs/matched_pair/E_s20260911/multidc_gtrxl_training/PPO_multidc_env_7e477_00000_0_2026-09-09_16-31-14/`

| 文件（各自运行根目录下） | SHA256 |
|---|---|
| V result.json（上列 PPO 目录下） | decda47b89bcbfe69b53f3f31f7dfec36daec6d74bf5824c322a1cdb141db89e |
| E result.json（上列 PPO 目录下） | c09a50eb54d7da169df0e4a3d2e681bf2d9210af393be850ddf04e936a773641 |
| V training_metrics.csv | a3277fb1c30213b287cdf859b86dcb889a5de8ece9557a9cda32171459ab7c0f |
| E training_metrics.csv | 384761096b8cc81ae37e6d8f0424f070589270a6dcd5f54b45bb72bb6ab5ef6b |
| V monitor.csv | dfe7c44224572c5e958474f74f5bb43107f878aa7751fb8936460609b89be555 |
| E monitor.csv | 78d0a9056b19de2e140803f84958a698d7755934e3b1d4a730e9c62088b384bb |
| V experiment_config.yml | a3f7ee71504b0f5d95cecd026bd26277f54fd358fc59a05b5a8fd45886fe855b |
| E experiment_config.yml | a1b918694c8fba2818ae8ea3410f5e7ba5ccd134602c93f9ea838fff41c3bc99 |
