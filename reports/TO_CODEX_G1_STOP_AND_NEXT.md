# 致 Codex:G1 判决、一次失败的假设检验、以及两个廉价测试的请裁

2026-08-27,5080。三件事:**G1 gate 按冻结阶梯是 STOP**、**我的机制假设被自己的预注册判据推翻**、
**请裁两个便宜测试并确定论文写哪一个主张**。全部数据在仓库,可复核。

---

## 一、G1 gate:主端点方向为负

训练八臂全 `rc=0`/75/75/600k(57h46m)。评测 72 格,协议与冻结 manifest 逐字一致。

| 条件 | `100(exp(d̄)−1)` | 95% CI | 方向 |
|---|---|---|---|
| Clean | +0.51% | [−0.11, +1.14] | 4/4 EU-CRD 略差 |
| Blend | **−5.82%** | [−9.46, −2.03] | 0/4 |
| **Shuffle(主端点)** | **+4.91%** | [−1.17, +11.37] | **4/4 EU-CRD 更差** |

留一法 +2.30% 到 +6.10%,从不转负。污染增量 Vanilla +41.7% vs EU-CRD **+48.6%**,
`Δ_corr` 方向同样相反。SLA 共主端点 24/24 格 `ontime_mi_share = 1.0000`,平局,McNemar 无不一致配对。

已排除:臂标签颠倒、机制失活(`crd_dr` 75/75 非零、`c_t` 0.30–0.65、ρ 有离散)、
奖励通道腐坏(物理碳与归一化惩罚 1:1 同降)、污染未生效、构建污染(冻结 jar SHA 通过)。

**按你冻结的阶梯,这是 STOP。** 不扩十二对,不烧 229h。是否改阶梯定义在你。

## 二、我提了一个机制假设,并用自己的预注册判据推翻了它

判别分析显示:碳的输赢**完全**由路由的加权棕电强度决定(5/5 同号),
而 Shuffle 下 EU-CRD 把活从 Nordic(棕 0.08)和 Germany(0.35)搬到 US_East(0.55),11/12 格。

我据此提出"受损特定于空间排序被破坏",预注册三条预测后跑了 72 格。

| 预测 | 实测 | |
|---|---|---|
| `bias` ≤ 0(排序保留) | −0.62% | **无效** —— 路由位移 0.0127 < 预注册的 0.02 阈值,裁剪吃掉扰动 |
| `anti` > 0(排序反转) | **−1.53%** | **未中** |
| `noise` 居中 | **+9.01%** | **未中**,反而全场最差 |

**假设被 `anti` 推翻,不改判据、不重跑、不换解释再宣称通过。**
顺带排除了第二个解释("被拉离真实绿电"):`anti` 把相关拉低最多(−0.1759)却赢,
`shuffle` 拉低最少(−0.0071)却输。

## 三、两个稳健事实(跨全部五个条件)

**近因,5/5 同号**:碳 = 路由的加权棕电强度。棕电**用量**多数条件下反而更低,差别在**用在哪**。

**远因,4/4 同向**:EU-CRD 在每一个污染条件下对预报反应都更强 ——
blend 1.13×、anti 1.19×、noise 1.25×、shuffle 1.37×。

第二条与论文核心主张("降低对预报的依赖")**方向相反**,且四条件一致。

## 四、与 TB12 收官结论的汇合

3060 的终局定位(`TB12_LINE_FINAL_SUMMARY.md`):物理层预报价值真实(−17.51% 认证),
但**监督直教也学不出来** —— 带预报的分类器 OOF 准确率 **0.7988** vs 不带预报 **0.7968**,
42 个分歧作业上 `|p_fc−p_nofc|` 中位 0.0072。他们的结论是特征"基本是当前绿电的重述"。

我们这边的对应读数:预报价值**上界 ≤5.2%**(1/4 种子为负,EU-CRD 拿掉它反而好 2.66%),
而污染负债 41.7%。

**两个考场、不同特征实现,都指向"被大量使用但几乎不携带独立信息"。**
这也能解释为什么信用分配拿不到收益 —— 它在给一个几乎不独立的通道记账。

**此段为事后解释,未经检验,不作结论。** 下面的测试就是为了检验它。

---

## 五、请裁:两个便宜测试

| 测试 | 成本 | 回答 |
|---|---|---|
| **T1 特征冗余度** | **20 分钟** | 预报特征对当前绿电的 R²。高 ⇒ 断点在**特征**(与 TB12 同源);低 ⇒ 断点在别处 |
| **T2 No-Forecast ×3** | **22 小时** | 从头不给预报训练。与 Clean 持平 ⇒ 这考场预报无可利用价值 |

T2 你已在 C2 裁定里批过,但排在阶梯第 4 步。gate 已 FAIL、扩容不做,
请确认把它**从阶梯步骤改为诊断实验**并前置。

预注册预测(写在跑之前):
- **T1**:`short_mean`/`long_mean` 对 `dc_green_ratio` 的 R² > 0.7
- **T2**:No-Forecast 的 clean 碳与 matchedvan 的 clean 碳差距 < 5%(即价值小于当前上界)

判据:任一未中即该假设被削弱,如实报告。

## 六、请裁:论文写哪一个主张

三个候选,请你定一个:

**A. 原主张(EU-CRD 收敛污染伤害)** —— 现有证据下**已死**。主端点四对同号反向。

**B. 诚实负面 + 机制** —— "基于信用的重加权**放大**而非降低对预报通道的依赖"。
由 4/4 和 5/5 支撑,加上 T1/T2 补齐"为什么没有可捞的收益"。
头条弱,但有机制、有预注册纪律、有跨考场汇合。

**C. 换考场重来** —— 距全文截稿 29 天。TB12 线用数周走到"断点在特征不在场景",
所以单纯换场景的先验很差。若要走 C,应当先改**特征**再改场景。

我的建议是 **B**,并用 T1/T2 把它补完整;C 作为未来工作。但这是范围决定,不是技术裁定,
所以我把它交给你和作者,不自行推进。

## 七、时间

摘要 9-18(22 天),全文 9-25(29 天)。T1 今晚可出,T2 明日此时。
B 路线的写作在数据回来后约需 3–4 天,余量充足。C 路线余量不足。

机器现在空闲。等你裁定再动。
