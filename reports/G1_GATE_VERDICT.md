# G1 futility gate 判决 —— 主端点方向为负

2026-08-26。训练 08-23 21:37 → 08-26 07:23,八臂全部 `rc=0` / 75/75 / 600k。
评测 08-26 08:56 → 13:23,72 格全部完成,环境哨兵全过。
判据、窗口、估计量、种子表在 `reports/G1_FREEZE_MANIFEST.md`,**先于数据冻结**。

## 主端点:Shuffle 碳,种子级配对 log-ratio

估计量 `d_i = log(C_EU / C_Van)`(三窗先池化 Σcarbon/Σcompleted_MI),报 `100(exp(d̄)−1)`。
**正值 = EU-CRD 排碳更多。**

| 条件 | 效应 | 95% CI | 方向一致性 |
|---|---|---|---|
| Clean | **+0.51%** | [−0.11, +1.14] | 4/4 EU-CRD 略差 |
| Blend | **−5.82%** | [−9.46, −2.03] | 0/4 EU-CRD 更差 |
| **Shuffle(主端点)** | **+4.91%** | [−1.17, +11.37] | **4/4 EU-CRD 更差** |

论文声称 EU-CRD 在预报被相干污染时**收敛伤害**,即 Shuffle 上 d 应为**负**。
实测为正,而且**四对全部同号**。

留一法确认不由单个种子驱动:

```
全部 +4.91%   去 s101 +6.10%   去 s102 +5.60%   去 s103 +5.69%   去 s104 +2.30%
```

## 污染增量:方向同样相反

| 臂 | Clean | Shuffle | 增量 |
|---|---|---|---|
| Vanilla | 0.0949 | 0.1345 | **+41.7%** |
| EU-CRD | 0.0953 | 0.1416 | **+48.6%** |

论文的 `Δ_corr` 论证要求 EU-CRD 的增量**更小**。实测更大。

## SLA 共主端点:平局

`C_min` = 三窗 `ontime_mi_share` 的最小值。**24 格全部 1.0000**,两臂零违约。
无不一致配对,精确 McNemar 无证据可用。完成率不再是区分维度。

## 唯一为正的发现在对照条件上

Blend 上 EU-CRD 好 **5.82%**,CI 排除零,四对同号。
Blend 是**信息被移除**(论文自己定义的对照),Shuffle 是**信息被误导**(论文定义的目标失效)。

所以机制确实在做事,但它**帮的是对照条件,伤的是主端点**。

## 已排除的解释

- **臂标签未颠倒**:`eucrd` 四臂 `crd.enabled=True` 且 `rho_routing` 0.86–0.91,`van` 四臂 `False`。
- **机制未失活**:`crd_dr` 75/75 次非零,`c_t` 全程 0.30–0.65,`ρ_routing` 有真实离散。
- **奖励通道未腐坏**:五臂物理碳与归一化惩罚 1:1 同降(−9.7% 至 −14.7%)。
- **污染确实生效**:Shuffle 碳比 Clean 高 42–49%。
- **构建未污染**:冻结 jar SHA 校验通过,`FIXED` 归一化生效,封顶零命中。

## 判决

按冻结纪律,四对完成即二选一:停,或跑固定的剩余八对。
**不作效果声明,不调参,不重跑。**

主端点点估计方向为负、四对同号、且污染增量方向同样相反。
按 Codex 的阶梯,这是 **STOP** 的条件。是否扩容属于阶梯定义,裁定权在 Codex。

数据:`g1/eval/csv/`(72 格),日志 `g1/eval/logs/`,训练 `drl-manager/logs/g1_*`。
