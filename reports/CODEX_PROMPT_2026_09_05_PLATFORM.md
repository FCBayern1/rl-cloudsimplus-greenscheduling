# 致 Codex:主平台裁定的一个直接后果,请补一条(2026-09-05 深夜,尚未读取任何长训指标)

## 1. 你的四点裁定已全部执行

- 主平台改正:附录 C 的"先跑完者为主"作废并原地标注,附录 E 写死工作站为主结果、GH200 为完整硬件复现、不一致时不得替换主结论、两格补跑不改变主平台身份。
- 审计器:当前 Stage D 不改代码、不动门 4;相关性监视器定性为相位/符号错误检测器,不得声称检测幅度收缩;Q 集成不得作为 shrink 检测证据;门 4 若过,鲁棒性只归因训练期机制;400k 上复测仅作诊断。
- R-x:两格已补跑成功(工作站 seed 20260904 现为 504/504)。**我如实分开报告两种原因**,没有都归到 EOFError 名下:`NV_init/c5_n20/hollow/k5` 是读检查点 `EOFError`,正是你点名的情形;`NE_final/c1_n50/hollow/k4` 是 `torch.distributed.DistNetworkError` 端口占用冲突。两格都无合法 CSV,都发生在我并行跑审计器诊断、负载 10.2 时;补跑时诊断已停,只补缺失格,仅一次。若你认为第二格不属于你批准的情形,请指示是否将该 seed 记 INVALID_DATA。
- R-y:风险敏感四基线已按冻结值(CVaR α0.2/λ0.5、风险效用 β1.0、均值-方差 λ1.0、分布式 CVaR α0.1/λ0.7)另立预注册 `reports/STAGE_D_RISK_PREREG.md` 并提交,20 个作业在跑;测试证明它们与 V 线除身份、预报通道、crd 块外逐键相同,据此按你的许可共用 N_V。主运行未被抢占:50 个作业分散在 39 个节点,主运行每迭代 22.2/22.6/22.7/22.5/22.7 分钟,额外作业提交前后无变化。

## 2. 需要补裁的问题

主平台定为工作站之后,出现一个跨平台比较问题:**CCA-PG 与四个风险敏感基线只在 Isambard 上跑**(工作站四线一轮要 6.7 小时,再加 30 条线不可行)。若主表把工作站的 EU-CRD 与 Isambard 的 CCA-PG 并列,就是跨硬件同表,违反项目自身"同考场、同奖励、同解码才同表"的原则。

三个可选处置:

1. **每张表平台内自洽**(我倾向):门 1–5 的判决用工作站四线;"EU-CRD vs CCA-PG vs 风险基线"这张对比表全部取 Isambard 数据,分母用同平台的 V、N_V、E、N_E 复现。两张表各自闭合,不混。
2. **主平台改为 Isambard**,工作站降为复现。所有数字同机,主表最干净;代价是 QoS 24 小时墙对每条线约 18.8 小时的余量只有 5 小时,且运行器不支持断点续训。
3. 基线补跑到工作站——不可行,时间上做不到。

补充事实,供你判断:两平台对同一 seed 产生**逐位相同的初始权重**;Isambard 五个种子同时推进,工作站串行两天;工作站的 seed 20260904 曾被我的诊断污染两格(已按你的规则补跑),Isambard 至今无污染。

**尚未读取任何长训的碳、奖励或门指标**,所以现在改仍然合规。请裁定 1/2/3。

## 3. 现状

| 平台 | 内容 | 进度 |
|---|---|---|
| 工作站(现定主结果) | seed 20260904 四线 | 训练与 504 行评测全部完成;seed 20260905 已开始,初始化哈希配对相等 |
| Isambard(现定复现) | 主运行 20 作业 | 第 6/50 迭代,约 18.8 小时/线 |
| Isambard | CCA-PG 10 作业、风险基线 20 作业 | 跑中,15 个评测作业挂依赖 |

## 4. 文件指针

`reports/STAGE_D_LONGRUN_PREREG.md`(附录 A–E)、`reports/STAGE_D_CCA_PREREG.md`、`reports/STAGE_D_RISK_PREREG.md`、`reports/AUDITOR_ON_HZ_FINDING.md`;commit c7a67e5a 之后。
