# 预注册:TB12 表示能力审计(Codex 2026-08-26 批准)

**身份**:这是**新的、独立的表示能力审计**,**不是**小样本 BC 门的重判。
旧判决 `GATE_BC_PROBE_VERDICT.md`(c7dd967)的 **FAIL 永久保留**,最终措辞为
"小样本 BC 探针 FAIL;不能证明映射可泛化"。本审计**无论通过与否,都不得
改写**既有 PPO 负结果(v3)与小样本 BC 负结果。

**它解决的唯一问题**:已登记的样本量混淆 —— 11 个分歧作业不足以区分
"预报表示无效"与"探针样本不足"。

**本文在产生任何结果之前落档冻结。全程禁止读取 T116+117。**

## 已接受的两处定性更正(Codex)

1. "gate-on-q 准确率更高"只说明 q 对**总体标签**有信息;总体标签大部分可由
   **当前绿电**解释,**不能**据此推出 q 保留了**预报增量**。
2. 关键量是**分歧作业上的 fc−nofc 差异**;该量当前为负且留一泛化失败,
   因此**现在不能说 trunk 已排除嫌疑**。
原报告中相关表述已就地撤回。

## 冻结的数据与切分

- 分布:**仅 T100+101/2021**。held-out T116+117 绝不读取。
- offset:**60 个互不重叠、全年均匀**,规则 `offset_i = floor(i*52262/60)`,
  i=0..59;实测最小间隔 **871** 行 > 每集 300 行 ⇒ 非重叠。
  清单落档于 `calib/tb12_repaudit_offsets.json`
  (sha256 `07431f34a41afdf2…`,**先于结果提交**)。
- 切分:按时间**连续 5 块**,每块 12 个 offset(块 k = offsets[12k:12k+12]);
  **blocked 5-fold out-of-fold**:第 k 折在其余 4 块上训练,只对第 k 块产出
  **out-of-fold 预测**。全部主统计量均来自 OOF 预测。
- 对称性:fc / nofc 共用**同一批 offset、同一折划分、同一套 teacher 标签、
  同样优化步数与 seed**(seed=20260826);唯一差异 forecast_mode;
  共用同一个起始 ck0;驱动同为冻结 greenfollow;
  `assert_corpora_aligned` 四项逐位哨兵在训练前核验。

## 主统计量(只在分歧作业上)

对每个 **clair ≠ greenfollow** 的分歧作业 j(取其首次 eligible 决策点):

    s_j = (2·y_j − 1) · (p_fc,j − p_nofc,j)

其中 y_j = 1 表示 **clair 认为应当等待**;p 为该臂的 **OOF** p_hold 预测。

## 冻结门(全过才算 PASS)

| # | 判据 |
|---|---|
| G1 | OOF 池化 **E[s] ≥ +0.05** |
| G2 | 5 个时间块中**至少 4 块** E[s] 为正 |
| G3 | **block bootstrap 95% CI 下界 > 0**(按块重采样,10000 次,seed 20260826) |
| G4 | **两臂均不退化**(OOF 预测的 argmax 两类各占 ≥5%) |
| — | 总体准确率、gate-on-q 拟合**仅作描述**,不得替代分歧集判据,不得据以宣称 trunk 无罪 |

## 分支(Codex 冻结)

- **仍 FAIL** ⇒ **正式关闭当前预报表示 / PPO 线**。结论:现有 forecast 通道
  在真正需要预报的作业上**没有可泛化增量**。
- **PASS** ⇒ 仅说明"**监督条件下映射可学**",**不改写** PPO 负结果;
  随后**另立预注册**进入 matched gate-only BC + 常驻锚。
- 两种结果**均不读取 T116+117**。

## 执行与冻结项

- jar / config:沿用 v3 冻结基线(jar `940078777d788d68…`,source `13348ae5673d`,
  config `2f31042f5e38adba…`)。审计不触碰 Java、不训练环境。
- corpus:`calib/tb12_repaudit_corpus_{fc,nofc}.npz`(哈希在执行回报中落档)。
- 超参沿用小样本探针:steps=2000, lr=1e-3, batch=256, seed=20260826,
  两臂逐位相同。
- 构建成本:60 offset × 2 臂,约每集 1–2 分钟,预计 2–4 小时。
